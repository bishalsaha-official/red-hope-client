import logo from '../../../assets/home/banner.jpg'

const Banner = () => {
    return (
        <div style={{backgroundImage: `url(${logo})`}} className='p-10 min-h-screen bg-center bg-no-repeat bg-cover'>
            
        </div>
    );
};

export default Banner;